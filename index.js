/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import IShopApp from './src/IShopApp';

AppRegistry.registerComponent(appName, () => IShopApp);
